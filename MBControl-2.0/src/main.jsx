import React from 'react'
import ReactDOM from 'react-dom/client'
import  MBControl from './MBControl.jsx'
import '../src/styles/styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MBControl/>
  </React.StrictMode>,
)
